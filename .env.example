# simulation-backend — copy to .env for local overrides (never commit .env).
# All values are optional; fixed runtime defaults are shown.

# The HTTP port is fixed at 19001 in source; PORT is intentionally ignored.
# 127.0.0.1 keeps the service private behind the VPS reverse proxy.
HOST=127.0.0.1

# Single allowed browser origin for CORS (the simulation-frontend dev server).
# CORS is not authentication.
FRONTEND_ORIGIN=http://localhost:3000

# Maximum JSON request body (Express/body-parser size string).
JSON_BODY_LIMIT=100kb

# Graceful-shutdown deadline before open connections are force-closed (ms).
SHUTDOWN_TIMEOUT_MS=10000

# SQLite database file (created on first migrate/start; the data/ directory is
# git-ignored). Relative paths resolve from the working directory (repo root).
DATABASE_PATH=data/simulation.sqlite

# How long a connection waits on a competing SQLite lock before failing (ms).
SQLITE_BUSY_TIMEOUT_MS=5000
